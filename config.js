var config = {
    development: {
        database: {
            host: 'localhost',
            db: 'raffle',
            user: 'root',
            password: 'password'
        },
        server: {
            host: 'localhost',
            port: '3000'
        }
    }
};
module.exports = config;